import {useState, useEffect} from 'react'
import * as spotifyAPI from "../../utilities/spotify-api"

export default function Homepage({user}) {

    // useEffect(function (){
    //     async function getSpotProfile() {
    //         const profile = await spotifyAPI.getProfile(user.)
    //     }
    // },[])

    return ( 
        <h1>homepage</h1>
    )
}